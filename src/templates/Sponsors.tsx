import { Section } from '@/layout/Section';

const Sponsors = () => (
  <Section
    title="utilise NDIS funding to receive care"
    description="Choose your support worker from our database of local support workers"
    // eslint-disable-next-line react/no-children-prop
    children=""
  ></Section>
);

export { Sponsors };
